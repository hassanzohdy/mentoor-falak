_Component({
                selector: 'markdown',
                c: 'Markdown', // c for component name
                unique: false, // flag to determine if the component object will be unique or not
                observable: true,
                observe: ['content','className','id','render'],
                children: {},
                render: function (component) {
                    let cndB2u_ = component.content;
this.setState('stapv4T', cndB2u_);
if (cndB2u_) { 
let elXIvCJ = eo('div','lzTf',null,`id`,`${component.id}`,`class`,`mdContent ${ component.className }`);
component.markdownContainer = elXIvCJ;
html(`${component.render(component.content)}`);
ec('div');
}
                    this.isReadyToGo();
                }
        });