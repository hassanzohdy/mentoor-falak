_Component({
                selector: 'flk-datepicker',
                c: 'Datepicker', // c for component name
                unique: false, // flag to determine if the component object will be unique or not
                observable: true,
                observe: ['name','id','placeholder','inputValue','isRequired'],
                children: {},
                render: function (component) {
                    let elkytaP = eo('div',null,null,`class`,`datepicker-wrapper`);
let el_zCV2 = eo('div',null,null,`class`,`form-group`);
el_zCV2.cls = {'group-error': !!cfrmdlr.getError(component.name) };

            for (let className in el_zCV2.cls) {
                el_zCV2.classList.toggle(className, el_zCV2.cls[className]);
            }  
            let elQJFZ7 = eo('label',null,null,`for`,`${component.id}`);
text(trans(component.inputs.getOption('label')));
let cnd9qDg = component.isRequired;
this.setState('stHqogw', cnd9qDg);
if (cnd9qDg) { 
let elwahMV = eo('span','CQpf',null,`title`,`${trans(`required`)}`,`class`,`required`);
text(`*`);
ec('span');
}ec('label');
let el32b_v = ev('input',null,null, eventListeners, {oninput:[function(e) {component.inputValue = this.value;},function(e) {let value = this.value.trim();if (component.isRequired && Is.empty(value)) {return cfrmdlr.addError(component.name, 'required', trans('validation.required'));}return cfrmdlr.removeError(component.name);}]},`value`,`${fval(component.inputValue)}`,`id`,`${component.id}`,`name`,`${(component.name).toInputName()}`,`placeholder`,`${component.placeholder}`,`type`,`text`,`class`,`${ component.inputs.getOption('class', '') }`);
component.input = el32b_v;
el32b_v.value = fval(component.inputValue);
let cndye0B = cfrmdlr.getError(component.name);
this.setState('stukjDd', cndye0B);
if (cndye0B) { 
let elGv6DC = eo('div','b7gf',null,`class`,`error-msg`);
text(cfrmdlr.getError(component.name));
ec('div');
}ec('div');
ec('div');

                    this.isReadyToGo();
                }
        });