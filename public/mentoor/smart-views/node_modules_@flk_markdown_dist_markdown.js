_Component({
                selector: 'markdown',
                c: 'Markdown', // c for component name
                unique: false, // flag to determine if the component object will be unique or not
                observable: true,
                observe: ['content','className','id','render'],
                children: {},
                render: function (component) {
                    let cndXgyu = component.content;
this.setState('stzkrCb', cndXgyu);
if (cndXgyu) { 
let eld2U4e = eo('div','drdf',null,`id`,`${component.id}`,`class`,`mdContent ${ component.className }`);
component.markdownContainer = eld2U4e;
html(`${component.render(component.content)}`);
ec('div');
}
                    this.isReadyToGo();
                }
        });