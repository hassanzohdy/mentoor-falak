_Component({
                selector: 'markdown',
                c: 'Markdown', // c for component name
                unique: false, // flag to determine if the component object will be unique or not
                observable: true,
                observe: ['content','className','id','render'],
                children: {},
                render: function (component) {
                    let cndYiNy = component.content;
this.setState('stOvbgF', cndYiNy);
if (cndYiNy) { 
let eln6eoo = eo('div','ZhBf',null,`id`,`${component.id}`,`class`,`mdContent ${ component.className }`);
component.markdownContainer = eln6eoo;
html(`${component.render(component.content)}`);
ec('div');
}
                    this.isReadyToGo();
                }
        });