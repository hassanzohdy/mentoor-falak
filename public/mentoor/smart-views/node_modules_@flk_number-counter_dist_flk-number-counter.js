_Component({
                selector: 'flk-number-counter',
                c: 'FlkNumberCounter', // c for component name
                unique: false, // flag to determine if the component object will be unique or not
                observable: true,
                observe: ['element','currentNumber'],
                children: {},
                render: function (component) {
                    let elwFSLe = eo('div',null,null, 'style', this.__info.style || {}, eventListeners, mergeEvents({onready:[function(e) {var $el = this;component.element = $el}]}, this.__info.events), boolAttrs, Object.merge({}, this.__info.boolAttrs || {}),...normalAttrs(Object.merge({}, this.__info.attrs || {})));
text(component.currentNumber);
ec('div');

                    setTimeout(function () {
                        let $el = elwFSLe;
                        component.element = $el;
                    }, 20);                
                
                    this.isReadyToGo();
                }
        });