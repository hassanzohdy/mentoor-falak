_Component({
                selector: 'flk-audio-player',
                c: 'AudioPlayer', // c for component name
                unique: false, // flag to determine if the component object will be unique or not
                observable: true,
                observe: ['play','src'],
                children: {},
                render: function (component) {
                    let elHZXU1 = eo('div',null,null,`class`,`audio-player-wrapper`);

                // let elHZXU1 = currentElement();

                if (! elHZXU1.__rendered) {
                    elHZXU1.__rendered = true;let el1Dy0O = eo('audio',null,null, eventListeners, {onready:[function(e) {var $el = this;component.play($el)}]},`preload`,`none`);
let elmzGem = eo('source',null,null,`src`,`${component.src}`);
ec('source');
ec('audio');

                    setTimeout(function () {
                        let $el = el1Dy0O;
                        component.play($el);
                    }, 20);                
                
                } else {
                    skip();
                }
            ec('div');

                    this.isReadyToGo();
                }
        });