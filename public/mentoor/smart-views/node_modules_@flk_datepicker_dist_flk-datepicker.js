_Component({
                selector: 'flk-datepicker',
                c: 'Datepicker', // c for component name
                unique: false, // flag to determine if the component object will be unique or not
                observable: true,
                observe: ['name','id','placeholder','inputValue','isRequired'],
                children: {},
                render: function (component) {
                    let elGwC76 = eo('div',null,null,`class`,`datepicker-wrapper`);
let elrBSaN = eo('div',null,null,`class`,`form-group`);
elrBSaN.cls = {'group-error': !!cfrmdlr.getError(component.name) };

            for (let className in elrBSaN.cls) {
                elrBSaN.classList.toggle(className, elrBSaN.cls[className]);
            }  
            let elUoKSc = eo('label',null,null,`for`,`${component.id}`);
text(trans(component.inputs.getOption('label')));
let cndgMoY = component.isRequired;
this.setState('stoN_nJ', cndgMoY);
if (cndgMoY) { 
let el37MMv = eo('span','Hirf',null,`title`,`${trans(`required`)}`,`class`,`required`);
text(`*`);
ec('span');
}ec('label');
let elt6KcW = ev('input',null,null, eventListeners, {oninput:[function(e) {component.inputValue = this.value;},function(e) {let value = this.value.trim();if (component.isRequired && Is.empty(value)) {return cfrmdlr.addError(component.name, 'required', trans('validation.required'));}return cfrmdlr.removeError(component.name);}]},`value`,`${fval(component.inputValue)}`,`id`,`${component.id}`,`name`,`${(component.name).toInputName()}`,`placeholder`,`${component.placeholder}`,`type`,`text`,`class`,`${ component.inputs.getOption('class', '') }`);
component.input = elt6KcW;
elt6KcW.value = fval(component.inputValue);
let cndQUQw = cfrmdlr.getError(component.name);
this.setState('stvwrAC', cndQUQw);
if (cndQUQw) { 
let elcwmmZ = eo('div','wTTf',null,`class`,`error-msg`);
text(cfrmdlr.getError(component.name));
ec('div');
}ec('div');
ec('div');

                    this.isReadyToGo();
                }
        });