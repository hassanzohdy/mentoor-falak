_Component({
                selector: 'flk-datepicker',
                c: 'Datepicker', // c for component name
                unique: false, // flag to determine if the component object will be unique or not
                observable: true,
                observe: ['name','id','placeholder','inputValue','isRequired'],
                children: {},
                render: function (component) {
                    let elzKBTF = eo('div',null,null,`class`,`datepicker-wrapper`);
let el_uuXB = eo('div',null,null,`class`,`form-group`);
el_uuXB.cls = {'group-error': !!cfrmdlr.getError(component.name) };

            for (let className in el_uuXB.cls) {
                el_uuXB.classList.toggle(className, el_uuXB.cls[className]);
            }  
            let el7mHzW = eo('label',null,null,`for`,`${component.id}`);
text(trans(component.inputs.getOption('label')));
let cndU3Bm = component.isRequired;
this.setState('stL23Ca', cndU3Bm);
if (cndU3Bm) { 
let elJawfV = eo('span','abkf',null,`title`,`${trans(`required`)}`,`class`,`required`);
text(`*`);
ec('span');
}ec('label');
let elyvfkT = ev('input',null,null, eventListeners, {oninput:[function(e) {component.inputValue = this.value;},function(e) {let value = this.value.trim();if (component.isRequired && Is.empty(value)) {return cfrmdlr.addError(component.name, 'required', trans('validation.required'));}return cfrmdlr.removeError(component.name);}]},`value`,`${fval(component.inputValue)}`,`id`,`${component.id}`,`name`,`${(component.name).toInputName()}`,`placeholder`,`${component.placeholder}`,`type`,`text`,`class`,`${ component.inputs.getOption('class', '') }`);
component.input = elyvfkT;
elyvfkT.value = fval(component.inputValue);
let cndw4FK = cfrmdlr.getError(component.name);
this.setState('stI5udo', cndw4FK);
if (cndw4FK) { 
let eleRGTt = eo('div','7Blf',null,`class`,`error-msg`);
text(cfrmdlr.getError(component.name));
ec('div');
}ec('div');
ec('div');

                    this.isReadyToGo();
                }
        });