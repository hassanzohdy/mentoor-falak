_Component({
                selector: 'flk-mdb-checkbox',
                c: 'FlkMdbCheckbox', // c for component name
                unique: false, // flag to determine if the component object will be unique or not
                observable: true,
                observe: ['id','value','label'],
                children: {},
                render: function (component) {
                    let elvy7AM = eo('div',null,null,`class`,`flk-mdb-checkbox custom-control custom-checkbox`);
let eluUmRl = ev('input',null,null, 'style', this.__info.style || {}, eventListeners, mergeEvents({oninput:[function(e) {component.value = this.value;}]}, this.__info.events), boolAttrs, Object.merge({}, this.__info.boolAttrs || {}),...normalAttrs(Object.merge({value:`${fval(component.value)}`,id:`${component.id}`,type:'checkbox',class:'custom-control-input'}, this.__info.attrs || {})));
eluUmRl.value = fval(component.value);
let el9nGu1 = eo('label',null,null,`class`,`custom-control-label`,`for`,`${ component.id }`);
text( trans(component.label) );

        if (this.hasContent()) {
            this.content();
        }ec('label');
ec('div');

                    this.isReadyToGo();
                }
        });