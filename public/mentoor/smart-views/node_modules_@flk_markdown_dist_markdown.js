_Component({
                selector: 'markdown',
                c: 'Markdown', // c for component name
                unique: false, // flag to determine if the component object will be unique or not
                observable: true,
                observe: ['content','className','id','render'],
                children: {},
                render: function (component) {
                    let cnd37fA = component.content;
this.setState('stlcelK', cnd37fA);
if (cnd37fA) { 
let elNajTY = eo('div','EpFf',null,`id`,`${component.id}`,`class`,`mdContent ${ component.className }`);
component.markdownContainer = elNajTY;
html(`${component.render(component.content)}`);
ec('div');
}
                    this.isReadyToGo();
                }
        });