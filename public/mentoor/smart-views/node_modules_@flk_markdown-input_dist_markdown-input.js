_Component({
                selector: 'markdown-input',
                c: 'MarkdownInput', // c for component name
                unique: false, // flag to determine if the component object will be unique or not
                observable: true,
                observe: ['classNam','preview','value','label','id','name','placeholder','required','error'],
                children: {},
                render: function (component) {
                    let ell3kdQ = eo('div',null,null,`class`,`markdown-input {{ component.classNamّّ|| '' }`);

                // let ell3kdQ = currentElement();

                if (! ell3kdQ.__rendered) {
                    ell3kdQ.__rendered = true;let el7DOTl = eo('div',null,null,`class`,`form-group`);
el7DOTl.cls = {'group-error': !!cfrmdlr.getError(component.name) };

            for (let className in el7DOTl.cls) {
                el7DOTl.classList.toggle(className, el7DOTl.cls[className]);
            }  
            let elRJLnc = eo('label',null,null,`for`,`${component.id}`);
text(trans(component.label));
let cndb26Y = component.required;
this.setState('stYywij', cndb26Y);
if (cndb26Y) { 
let elbfi8v = eo('span','uEKf',null,`title`,`${trans(`required`)}`,`class`,`required`);
text(`*`);
ec('span');
}ec('label');
let elMHBOl = eo('textarea',null,null, eventListeners, {oninput:[function(e) {component.value = this.value;},function(e) {let value = this.value.trim();if (component.required && Is.empty(value)) {return cfrmdlr.addError(component.name, 'required', trans('validation.required'));}return cfrmdlr.removeError(component.name);}],onready:[function(e) {var $el = this;component.preview($el)}]},`value`,`${fval(component.value)}`,`id`,`${component.id}`,`name`,`${(component.name).toInputName()}`,`placeholder`,`${component.placeholder}`,`type`,`textarea`);
component.input = elMHBOl;
elMHBOl.value = fval(component.value);
ec('textarea');

                    setTimeout(function () {
                        let $el = elMHBOl;
                        component.preview($el);
                    }, 20);                
                let cndNX2n = cfrmdlr.getError(component.name);
this.setState('stSUaUh', cndNX2n);
if (cndNX2n) { 
let elknOmz = eo('div','rIrf',null,`class`,`error-msg`);
text(cfrmdlr.getError(component.name));
ec('div');
}ec('div');

                } else {
                    skip();
                }
            ec('div');
let cndxKxs = component.error;
this.setState('stv3tSr', cndxKxs);
if (cndxKxs) { 
let elqXAQ5 = eo('div','KGxf',null,`class`,`error-msg bold`);
text(component.error);
ec('div');
}
                    this.isReadyToGo();
                }
        });