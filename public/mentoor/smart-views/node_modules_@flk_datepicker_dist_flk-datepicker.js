_Component({
                selector: 'flk-datepicker',
                c: 'Datepicker', // c for component name
                unique: false, // flag to determine if the component object will be unique or not
                observable: true,
                observe: ['name','id','placeholder','inputValue','isRequired'],
                children: {},
                render: function (component) {
                    let elpOEfz = eo('div',null,null,`class`,`datepicker-wrapper`);
let elO7Afm = eo('div',null,null,`class`,`form-group`);
elO7Afm.cls = {'group-error': !!cfrmdlr.getError(component.name) };

            for (let className in elO7Afm.cls) {
                elO7Afm.classList.toggle(className, elO7Afm.cls[className]);
            }  
            let elMZJuh = eo('label',null,null,`for`,`${component.id}`);
text(trans(component.inputs.getOption('label')));
let cnddi0l = component.isRequired;
this.setState('sta4Qx7', cnddi0l);
if (cnddi0l) { 
let elq0Cf6 = eo('span','FIPf',null,`title`,`${trans(`required`)}`,`class`,`required`);
text(`*`);
ec('span');
}ec('label');
let elEoChG = ev('input',null,null, eventListeners, {oninput:[function(e) {component.inputValue = this.value;},function(e) {let value = this.value.trim();if (component.isRequired && Is.empty(value)) {return cfrmdlr.addError(component.name, 'required', trans('validation.required'));}return cfrmdlr.removeError(component.name);}]},`value`,`${fval(component.inputValue)}`,`id`,`${component.id}`,`name`,`${(component.name).toInputName()}`,`placeholder`,`${component.placeholder}`,`type`,`text`,`class`,`${ component.inputs.getOption('class', '') }`);
component.input = elEoChG;
elEoChG.value = fval(component.inputValue);
let cnd74F7 = cfrmdlr.getError(component.name);
this.setState('stI6NE2', cnd74F7);
if (cnd74F7) { 
let elr7gqu = eo('div','mRuf',null,`class`,`error-msg`);
text(cfrmdlr.getError(component.name));
ec('div');
}ec('div');
ec('div');

                    this.isReadyToGo();
                }
        });