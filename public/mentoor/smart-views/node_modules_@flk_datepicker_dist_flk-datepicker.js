_Component({
                selector: 'flk-datepicker',
                c: 'Datepicker', // c for component name
                unique: false, // flag to determine if the component object will be unique or not
                observable: true,
                observe: ['name','id','placeholder','inputValue','isRequired'],
                children: {},
                render: function (component) {
                    let elMzFnJ = eo('div',null,null,`class`,`datepicker-wrapper`);
let elSuL2w = eo('div',null,null,`class`,`form-group`);
elSuL2w.cls = {'group-error': !!cfrmdlr.getError(component.name) };

            for (let className in elSuL2w.cls) {
                elSuL2w.classList.toggle(className, elSuL2w.cls[className]);
            }  
            let elTUIVm = eo('label',null,null,`for`,`${component.id}`);
text(trans(component.inputs.getOption('label')));
let cnddsDl = component.isRequired;
this.setState('stkQbDo', cnddsDl);
if (cnddsDl) { 
let el79gQH = eo('span','qcqf',null,`title`,`${trans(`required`)}`,`class`,`required`);
text(`*`);
ec('span');
}ec('label');
let elYL0a8 = ev('input',null,null, eventListeners, {oninput:[function(e) {component.inputValue = this.value;},function(e) {let value = this.value.trim();if (component.isRequired && Is.empty(value)) {return cfrmdlr.addError(component.name, 'required', trans('validation.required'));}return cfrmdlr.removeError(component.name);}]},`value`,`${fval(component.inputValue)}`,`id`,`${component.id}`,`name`,`${(component.name).toInputName()}`,`placeholder`,`${component.placeholder}`,`type`,`text`,`class`,`${ component.inputs.getOption('class', '') }`);
component.input = elYL0a8;
elYL0a8.value = fval(component.inputValue);
let cndxslB = cfrmdlr.getError(component.name);
this.setState('stUScHS', cndxslB);
if (cndxslB) { 
let el11g7O = eo('div','bDBf',null,`class`,`error-msg`);
text(cfrmdlr.getError(component.name));
ec('div');
}ec('div');
ec('div');

                    this.isReadyToGo();
                }
        });