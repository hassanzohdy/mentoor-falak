_Component({
                selector: 'markdown-input',
                c: 'MarkdownInput', // c for component name
                unique: false, // flag to determine if the component object will be unique or not
                observable: true,
                observe: ['classNam','preview','value','label','id','name','placeholder','required','error'],
                children: {},
                render: function (component) {
                    let elkxetY = eo('div',null,null,`class`,`markdown-input {{ component.classNamّّ|| '' }`);

                // let elkxetY = currentElement();

                if (! elkxetY.__rendered) {
                    elkxetY.__rendered = true;let elLOLeH = eo('div',null,null,`class`,`form-group`);
elLOLeH.cls = {'group-error': !!cfrmdlr.getError(component.name) };

            for (let className in elLOLeH.cls) {
                elLOLeH.classList.toggle(className, elLOLeH.cls[className]);
            }  
            let eldaOWY = eo('label',null,null,`for`,`${component.id}`);
text(trans(component.label));
let cndXYY2 = component.required;
this.setState('stp36Gb', cndXYY2);
if (cndXYY2) { 
let elHo9Ya = eo('span','c5if',null,`title`,`${trans(`required`)}`,`class`,`required`);
text(`*`);
ec('span');
}ec('label');
let elIJZGR = eo('textarea',null,null, eventListeners, {oninput:[function(e) {component.value = this.value;},function(e) {let value = this.value.trim();if (component.required && Is.empty(value)) {return cfrmdlr.addError(component.name, 'required', trans('validation.required'));}return cfrmdlr.removeError(component.name);}],onready:[function(e) {var $el = this;component.preview($el)}]},`value`,`${fval(component.value)}`,`id`,`${component.id}`,`name`,`${(component.name).toInputName()}`,`placeholder`,`${component.placeholder}`,`type`,`textarea`);
component.input = elIJZGR;
elIJZGR.value = fval(component.value);
ec('textarea');

                    setTimeout(function () {
                        let $el = elIJZGR;
                        component.preview($el);
                    }, 20);                
                let cndzOuH = cfrmdlr.getError(component.name);
this.setState('stMUl4R', cndzOuH);
if (cndzOuH) { 
let elZcM7Y = eo('div','gZsf',null,`class`,`error-msg`);
text(cfrmdlr.getError(component.name));
ec('div');
}ec('div');

                } else {
                    skip();
                }
            ec('div');
let cndVCvk = component.error;
this.setState('stDR2QY', cndVCvk);
if (cndVCvk) { 
let elCPyAu = eo('div','fSJf',null,`class`,`error-msg bold`);
text(component.error);
ec('div');
}
                    this.isReadyToGo();
                }
        });