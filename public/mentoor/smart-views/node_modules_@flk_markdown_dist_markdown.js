_Component({
                selector: 'markdown',
                c: 'Markdown', // c for component name
                unique: false, // flag to determine if the component object will be unique or not
                observable: true,
                observe: ['content','className','id','render'],
                children: {},
                render: function (component) {
                    let cnd37E2 = component.content;
this.setState('strFwVf', cnd37E2);
if (cnd37E2) { 
let elm0osS = eo('div','x5Ff',null,`id`,`${component.id}`,`class`,`mdContent ${ component.className }`);
component.markdownContainer = elm0osS;
html(`${component.render(component.content)}`);
ec('div');
}
                    this.isReadyToGo();
                }
        });