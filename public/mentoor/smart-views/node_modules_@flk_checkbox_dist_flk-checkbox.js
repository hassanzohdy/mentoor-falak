_Component({
                selector: 'flk-checkbox',
                c: 'FlkCheckbox', // c for component name
                unique: false, // flag to determine if the component object will be unique or not
                observable: true,
                observe: ['id','theme','label'],
                children: {},
                render: function (component) {
                    let elRWPYj = ev('input',null,null, 'style', this.__info.style || {}, eventListeners, mergeEvents({}, this.__info.events), boolAttrs, Object.merge({}, this.__info.boolAttrs || {}),...normalAttrs(Object.merge({id:`${component.id}`,type:'checkbox',class:`checker ${ component.theme }`}, this.__info.attrs || {})));
let cndvlyf = component.label;
this.setState('stKcKac', cndvlyf);
if (cndvlyf) { 
let elesceP = eo('label','BTBf',null,`for`,`${ component.id }`);
text(trans(component.label));
ec('label');
}
                    this.isReadyToGo();
                }
        });